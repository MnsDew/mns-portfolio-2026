# OG image

Social previews use `public/logo-black.png` (see `lib/seo.ts`).

To switch assets, change `OG_IMAGE_PATH` in `lib/seo.ts` — e.g. `/web-app-manifest-512x512.png` or a custom `og-image.png` you add later.
